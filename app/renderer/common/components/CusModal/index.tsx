/**
 * @description 所有弹窗组件集合
 * */
import Index from "./CusDialog";
import CusConfirm from "./CusConfirm";

export const Dialog = Index;
export const Confirm = CusConfirm;

export default {
    Dialog: Index,
    Confirm: CusConfirm
}
